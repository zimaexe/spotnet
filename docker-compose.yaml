version: '3.8'

networks:
  app_network:
    driver: bridge

services:
  backend:
    build: .
    restart: always
    volumes:
      - ./entrypoint.sh:/app/entrypoint.sh
    env_file:
      - .env
    expose:
      - "8000"
    networks:
      - app_network

  frontend:
    build: ./frontend
    restart: always
    environment:
      - CHOKIDAR_USEPOLLING=true
    networks:
      - app_network

  nginx:
    restart: unless-stopped
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./certs/spotnet.xyz.chain.crt:/etc/nginx/spotnet.xyz.chain.crt:ro
      - ./certs/spotnet.xyz.key:/etc/nginx/spotnet.xyz.key:ro
      - ./spotnet.conf:/etc/nginx/conf.d/spotnet.conf:ro
      - ./entrypoint.sh:/app/entrypoint.sh:ro
      - ./frontend:/usr/src/app:ro
    depends_on:
      - backend
      - frontend
    networks:
      - app_network